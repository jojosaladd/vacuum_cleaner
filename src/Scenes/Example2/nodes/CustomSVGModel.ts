import {ASerializable, ASVGModel} from "../../../anigraph";

/**
 * Remember to decorate any model classes with @ASerializable!
 */
@ASerializable("CustomSVGModel")
export class CustomSVGModel extends ASVGModel{

    /**
     * You could customize the behavior here. For now we are just rendering vector Lab Trash as a basic SVG object.
     */

}
